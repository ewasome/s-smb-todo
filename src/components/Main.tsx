import React from 'react';

import ListTabs from './ListTabs';
import ListView from './ListView';

const Main: React.FC = () => {
  return (
    <div>
      <ListTabs />
      <ListView />
    </div>
  );
};

export default Main;
